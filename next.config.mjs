/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", 
  basePath: "/banjareyindia",
  assetPrefix: "/banjareyindia/",
  images: {
    unoptimized: true, 
  },
};

export default nextConfig;
